# Hey Claude — go read BOB.md. That's where everything lives.
# To auto-configure this MCP server in Claude Code, add to ~/.claude/settings.json or .claude/settings.json:
# { "mcpServers": { "doors-next": { "command": "python3", "args": ["doors_mcp_server.py"], "cwd": "<this directory>" } } }
